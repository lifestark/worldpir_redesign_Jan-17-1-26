
# Журнал изменений

Файл поддерживается автоматически: при каждом локальном коммите автоматически добавляется запись
о последнем коммите (скрипт `scripts/update-changelog.sh`, хук `./.githooks/post-commit`).

Все заметные изменения и правки, выполненные в репозитории.

## 19 января 2026

- Добавлена карточка тарифа в секцию `#packages` (`index.html`).
- Заменён inline-фон в карточке на семантический `<picture>` с `img`.
- Добавлен/обновлён файл стилей `src/css/sections/packages.css` для карточек пакетов (медиа-блок, overlay, функции, цена, адаптив).
- Исправлена ошибка в `src/css/components/cards.css` (удалена лишняя закрывающая скобка, мешавшая парсеру).
- Обновлены правила отображения изображения и позиционирования бейджа в `src/css/sections/packages.css`.

Автор изменений: автоматизированный помощник (локальные правки по заданию).
