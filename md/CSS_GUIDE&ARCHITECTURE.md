# CSS Guide — Быстрый справочник

## 🎨 Цвета и переменные
Все глобальные настройки находятся в [src/css/base/variables.css](../src/css/base/variables.css).
- **Цвета**: Ищите переменные `--color-primary`, `--color-accent` и т.д.
- **Сетка**: Максимальная ширина контейнера — `--container-max-width`.
- **Шрифты**: Основные семейства и размеры — `--font-family-base`, `--font-size-base`.

## 📐 Где редактировать блоки?

| Задача | Файл |
| :--- | :--- |
| **Шапка и меню бургера** | [navigation.css](../src/css/components/navigation.css) |
| **Кнопки (все типы)** | [buttons.css](../src/css/components/buttons.css) |
| **Карточки (услуги, пакеты)** | [cards.css](../src/css/components/cards.css) |
| **Формы и инпуты** | [form.css](../src/css/components/form.css) |
| **Модальные окна** | [modal.css](../src/css/components/modal.css) |
| **Аниматоры (секция)** | [animators.css](../src/css/sections/animators.css) |
| **FAQ (аккордеон)** | [faq.css](../src/css/sections/faq.css) |
| **Футер** | [footer.css](../src/css/sections/footer.css) |

## 📱 Адаптивная верстка
Проект использует **Desctop First**.
- Базовые стили пишутся без медиа-запросов (для мобильных).
- Для планшетов и десктопов используются медиа-запросы в конце каждого файла:
```css
@media (min-width: 768px) { /* Планшеты */ }
@media (min-width: 1024px) { /* Десктоп */ }
```

## ➕ Добавление новой секции
1. Создайте файл `src/css/sections/my-block.css`.
2. Импортируйте его в `src/css/main.css`:
   `@import url('sections/my-block.css');`

## 🚀 Оптимизация
Для продакшена рекомендуется использовать минификаторы:
1. **Онлайн**: [cssminifier.com](https://cssminifier.com/)
2. **PostCSS**: `postcss src/css/main.css -o dist/style.min.css -u cssnano`

## 📝 Стиль комментариев
Все комментарии в проекте должны быть на русском языке. Это правило распространяется на CSS, JS, HTML и документацию. Исключение — очень короткие метки селекторов (например `/* nav */`), если это повышает читаемость. Пожалуйста, придерживайтесь этого правила при добавлении или правке стилей.



# CSS Архитектура

Проект использует модульный подход к организации CSS, основанный на принципах SMACSS и методологии BEM.

## 📂 Структура папок (`src/css/`)

### 1. `base/` — Базовые стили
Фундаментальные настройки и сбросы:
- **fonts.css** — Подключение шрифтов.
- **variables.css** — CSS переменные (цвета, сетка, отступы).
- **reset.css** — Сброс стандартных стилей браузера.
- **typography.css** — Базовые стили текста (заголовки, параграфы).
- **utilities.css** — Вспомогательные классы.

### 2. `layout/` — Компоновка
Глобальные структурные элементы:
- **container.css** — Ограничивающий контейнер.
- **section.css** — Общие стили для секций (padding, заголовки).

### 3. `components/` — Компоненты UI
Переиспользуемые и независимые элементы:
- **buttons.css** — Кнопки всех типов.
- **cards.css** — Карточки услуг и пакетов.
- **form.css** — Элементы форм.
- **modal.css** — Модальные окна.
- **navigation.css** — Шапка и меню.

### 4. `sections/` — Специфичные секции
Стили, привязанные к конкретным блокам страницы:
- `animators.css`, `benefits.css`, `cta.css`, `faq.css`, `footer.css`, `gallery.css`, `hero.css`, `packages.css`, `reviews.css`, `services.css`.

## � Принципы разработки

1. **BEM (Block Element Modifier)**: Всегда используйте `.block__element--modifier`.
2. **Desctop First**: Сначала стили для мобильных устройств, затем `@media` для mobile.
3. **CSS Variables**: Никаких "магических чисел" и цветов. Все должно быть в `root.css`.
4. **Один файл — один блок**: Не смешивайте стили разных компонентов в одном файле.

## 📥 Подключение (main.css)

Все файлы импортируются в [src/css/main.css](../src/css/main.css) через `@import`. В HTML подключается только `main.css`.

```css
@import url('base/rootcss');
@import url('base/fonts.css');
/* ... и так далее */
```

## 🚀 Оптимизация для Production

Для финальной сборки рекомендуется объединить и минифицировать CSS:

Обновлено: 18 января 2026 г.
