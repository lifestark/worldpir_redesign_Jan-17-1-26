# CSS Архитектура

Проект использует модульный подход к организации CSS, основанный на принципах SMACSS и методологии BEM.

## 📂 Структура папок (`src/css/`)

### 1. `base/` — Базовые стили
Фундаментальные настройки и сбросы:
- **fonts.css** — Подключение шрифтов.
- **variables.css** — CSS переменные (цвета, сетка, отступы).
- **reset.css** — Сброс стандартных стилей браузера.
- **typography.css** — Базовые стили текста (заголовки, параграфы).
- **utilities.css** — Вспомогательные классы.

### 2. `layout/` — Компоновка
Глобальные структурные элементы:
- **container.css** — Ограничивающий контейнер.
- **section.css** — Общие стили для секций (padding, заголовки).

### 3. `components/` — Компоненты UI
Переиспользуемые и независимые элементы:
- **buttons.css** — Кнопки всех типов.
- **cards.css** — Карточки услуг и пакетов.
- **form.css** — Элементы форм.
- **modal.css** — Модальные окна.
- **navigation.css** — Шапка и меню.

### 4. `sections/` — Специфичные секции
Стили, привязанные к конкретным блокам страницы:
- `animators.css`, `benefits.css`, `cta.css`, `faq.css`, `footer.css`, `gallery.css`, `hero.css`, `packages.css`, `reviews.css`, `services.css`.

## � Принципы разработки

1. **BEM (Block Element Modifier)**: Всегда используйте `.block__element--modifier`.
2. **Mobile First**: Сначала стили для мобильных устройств, затем `@media` для десктопа.
3. **CSS Variables**: Никаких "магических чисел" и цветов. Все должно быть в `variables.css`.
4. **Один файл — один блок**: Не смешивайте стили разных компонентов в одном файле.

## 📥 Подключение (main.css)

Все файлы импортируются в [src/css/main.css](../src/css/main.css) через `@import`. В HTML подключается только `main.css`.

```css
@import url('base/variables.css');
@import url('base/fonts.css');
/* ... и так далее */
```

## 🚀 Оптимизация для Production

Для финальной сборки рекомендуется объединить и минифицировать CSS:
- Используйте инструменты типа `cssnano`, `clean-css` или онлайн-компрессоры.
- Ссылка на [инструкцию по сборке](CSS_GUIDE.md#оптимизация).
